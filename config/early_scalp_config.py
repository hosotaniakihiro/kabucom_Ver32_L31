# ============================================================
# File   : config/early_scalp_config.py
# Version: Ver1.0-FINAL-EARLY-SCALP-PRODUCTION
# ------------------------------------------------------------
# EARLY_SCALP（早乗り早降り）専用設定
#
# ✔ 殿様イナゴとは完全分離
# ✔ ENTRY より EXIT を最優先
# ✔ AI は止め役（BLOCK 専用）
# ✔ HOLD 超短期（秒単位）
# ✔ SUMMARY / RANKING と安全に共存
# ============================================================

from datetime import time


# ============================================================
# EARLY SCALP メイン設定
# ============================================================

EARLY_SCALP_CONFIG = {

    # ========================================================
    # 時間帯ガード
    # ========================================================
    # ・寄り直後の歪みを取りに行く
    # ・後場もOKだが引け間際は除外
    # ========================================================
    "TIME_GUARD": {
        # この時刻より前は ENTRY しない
        "NO_ENTRY_BEFORE": time(9, 0),

        # この時刻以降は ENTRY しない
        "NO_ENTRY_AFTER": time(14, 30),
    },

    # ========================================================
    # ENTRY 条件（軽量・即断）
    # ========================================================
    "ENTRY": {
        # -------------------------------
        # 出来高スピード（直近30〜60秒）
        # -------------------------------
        # 1.0 = 平常
        # 1.5 = 明確な増加
        # 2.0 = 異常域
        "MIN_VOLUME_SPEED": 1.8,

        # -------------------------------
        # 超短期リターン（%）
        # -------------------------------
        # 例: 0.15 = +0.15%
        "MIN_FAST_RETURN": 0.15,

        # -------------------------------
        # 価格位置条件
        # -------------------------------
        # VWAP 上を必須にするか
        "REQUIRE_ABOVE_VWAP": True,

        # MA5 上を必須にするか（通常は False 推奨）
        "REQUIRE_ABOVE_MA5": False,

        # -------------------------------
        # 板・流動性
        # -------------------------------
        # 許容最大スプレッド（%）
        "MAX_SPREAD": 0.35,
    },

    # ========================================================
    # EXIT 条件（最重要）
    # ========================================================
    "EXIT": {
        # -------------------------------
        # 利確（%）
        # -------------------------------
        # 小さく、確実に
        "TAKE_PROFIT": 0.5,

        # -------------------------------
        # 損切り（%）
        # -------------------------------
        # 早く切るのが正義
        "STOP_LOSS": -0.4,

        # -------------------------------
        # 最大保有時間（秒）
        # -------------------------------
        # 60〜120秒が基本レンジ
        "MAX_HOLD_SECONDS": 90,

        # -------------------------------
        # 出来高失速判定
        # -------------------------------
        # この値を下回ったら勢い終了
        "MIN_VOLUME_SPEED": 1.0,

        # -------------------------------
        # VWAP 割れ即 EXIT
        # -------------------------------
        "EXIT_ON_VWAP_BREAK": True,
    },

    # ========================================================
    # リスク管理
    # ========================================================
    "RISK": {
        # -------------------------------
        # ロット比率
        # -------------------------------
        # 通常ロットに対する割合
        # 例: 0.3 = 30%
        "LOT_RATIO": 0.3,

        # -------------------------------
        # 同時ポジション数
        # -------------------------------
        # EARLY_SCALP は一点集中
        "MAX_SIMULTANEOUS": 1,
    },

    # ========================================================
    # ログ・検証用（売買判断には使わない）
    # ========================================================
    "LOGGING": {
        # ENTRY 時に詳細ログを出す
        "VERBOSE_ENTRY": True,

        # EXIT 理由を必ず記録
        "LOG_EXIT_REASON": True,

        # ENTRY から最初の N 秒の PnL を記録
        "TRACK_INITIAL_PNL_SECONDS": 10,
    },
}